# Aiven PostgreSQL (Kodbank)
# Option 1: Use full DB URL from Aiven service overview
DB_URL=postgres://avnadmin:YOUR_PASSWORD@your-project.aivencloud.com:PORT/defaultdb?sslmode=require

# Option 2: Use individual parameters
# PG_HOST=your-project.aivencloud.com
# PG_PORT=12345
# PG_USER=avnadmin
# PG_PASSWORD=your_password
# PG_DATABASE=defaultdb

# Optional: path to Aiven CA cert (ca.pem) for strict SSL verification
# PG_CA_PATH=./ca.pem

# JWT signing (use strong random value in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS (when frontend on different origin; optional in production)
# ALLOW_ORIGIN=http://localhost:5173
